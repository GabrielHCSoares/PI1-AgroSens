<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#00B050">

    <!-- Container Central: Logo Folha + Nome AgroSens -->
    <LinearLayout
        android:id="@+id/containerLogo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical"
        android:gravity="center">

        <!-- Logo Folha Verde Claro -->
        <ImageView
            android:id="@+id/imgLogoLeaf"
            android:layout_width="140dp"
            android:layout_height="180dp"
            android:src="@drawable/ic_logo_leaf"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter" />

        <!-- Nome AgroSens -->
        <TextView
            android:id="@+id/txtAppName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="AgroSens"
            android:textColor="#000000"
            android:textSize="36sp"
            android:fontFamily="sans-serif-light" />

    </LinearLayout>

    <!-- Container Inferior: Botões Log In e Sign Up -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingLeft="40dp"
        android:paddingRight="40dp"
        android:paddingBottom="48dp">

        <!-- Botão Log In -->
        <TextView
            android:id="@+id/btnAuthLogIn"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@drawable/bg_btn_black_rounded"
            android:text="Log In"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackground"
            android:layout_marginBottom="14dp" />

        <!-- Botão Sign Up -->
        <TextView
            android:id="@+id/btnAuthSignUp"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="@drawable/bg_btn_black_rounded"
            android:text="Sign Up"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackground" />

    </LinearLayout>

</RelativeLayout>
